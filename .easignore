# Dependencies
node_modules/
.expo/
dist/
web-build/
npm-debug.*

# Native build folders (Managed workflow handles these via prebuild)
ios
android

# Assets not needed for build
studio-tunisiska-services
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Git folder (don't upload to EAS - slows down build)
.git

# Vercel
.vercel

# Environment variables
# We explicitly allow .env.production so EAS Build sees your config variables.
# For maximum security, use EAS Secrets (eas secret:create) instead.
.env
.env.local
.env.production
# .env.preview (Add if you use it)
